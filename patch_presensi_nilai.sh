sed -i 's/let availableClasses = Array.from(new Set(\[\.\.\.assignedClasses\]))\.filter(Boolean)\.sort() as string\[\];/let availableClasses = Array.from(new Set([...assignedClasses, ...(user?.subjects || []).map((s: any) => s.className || s.class_name)])).filter(Boolean).sort() as string[];/' src/pages/GuruPages.tsx
sed -i 's/let availableClasses = Array.from(new Set(\[\.\.\.assignedClasses\]))\.sort() as string\[\];/let availableClasses = Array.from(new Set([...assignedClasses, ...(user?.subjects || []).map((s: any) => s.className || s.class_name)])).filter(Boolean).sort() as string[];/' src/pages/GuruPages.tsx
